<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:aop="http://www.springframework.org/schema/aop" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc" xmlns:p="http://www.springframework.org/schema/p"
       xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.1.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.1.xsd
		http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.0.xsd">

    <description>Spring MVC Configuration</description>

    <aop:aspectj-autoproxy proxy-target-class="true"/>

    <!-- 自动扫描且只扫描@Controller -->
    <context:component-scan base-package="com.ptsisi.daily.web.controller">
        <context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
    </context:component-scan>

    <bean id="supportedMediaTypes" class="java.util.ArrayList">
        <constructor-arg>
            <list>
                <value>text/plain;charset=UTF-8</value>
                <value>*/*</value>
            </list>
        </constructor-arg>
    </bean>

    <bean id="conversionService" class="org.springframework.format.support.FormattingConversionServiceFactoryBean"/>

    <!-- 使用缓存mvc注解配置 -->
    <mvc:annotation-driven>
        <!-- 配置StringHttpMessageConverter当@ResponseBody 时中文正常显示 -->
        <mvc:message-converters>
            <bean class="org.springframework.http.converter.StringHttpMessageConverter"
                  p:supportedMediaTypes-ref="supportedMediaTypes"/>
            <bean
                    class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter"
                    p:prettyPrint="true"/>
        </mvc:message-converters>
    </mvc:annotation-driven>

    <mvc:interceptors>
        <bean class="com.ptsisi.router.DefaultViewMapperInterceptor"/>
    </mvc:interceptors>

    <!-- 使用默认的servlet handler -->
    <mvc:default-servlet-handler/>

    <!-- 静态资源的访问 -->
    <mvc:resources location="/static/" mapping="/static/**"/>


    <bean id="preTemplateLoaders" class="java.util.ArrayList">
        <constructor-arg>
            <list>
                <ref bean="defaultTemplateLoader"/>
            </list>
        </constructor-arg>
    </bean>
    <bean id="freemarkerVariables" class="java.util.HashMap">
        <constructor-arg>
            <map>
                <entry key="xml_escape">
                    <bean class="freemarker.template.utility.XmlEscape"/>
                </entry>
            </map>
        </constructor-arg>
    </bean>

    <!-- freemarker的配置 -->
    <bean id="freemarkerConfigurer"
          class="org.springframework.web.servlet.view.freemarker.FreeMarkerConfigurer"
          p:defaultEncoding="UTF-8" p:templateLoaderPath="/pages/"
          p:preTemplateLoaders-ref="preTemplateLoaders"
          p:freemarkerVariables-ref="freemarkerVariables" p:configLocation="classpath:META-INF/freemarker.properties"/>

    <bean id="tagLibraryProviderFactory" class="com.ptsisi.ui.tagModel.TagLibraryProviderFactory">
        <constructor-arg>
            <map>
                <entry key="d" value-ref="defaultTagLibraryProvider"/>
            </map>
        </constructor-arg>
    </bean>

    <!-- FreeMarker视图解析 如返回userinfo。。在这里配置后缀名ftl和视图解析器。。 -->
    <bean id="viewResolver"
          class="org.springframework.web.servlet.view.freemarker.FreeMarkerViewResolver"
          p:cache="false" p:suffix=".ftl" p:contentType="text/html; charset=UTF-8"
          p:exposeRequestAttributes="true" p:exposeSessionAttributes="true"
          p:exposeSpringMacroHelpers="true" p:requestContextAttribute="rc"
          p:viewClass="com.ptsisi.view.DefaultFreeMarkerView"/>

    <bean id="messageSource"
          class="org.springframework.context.support.ResourceBundleMessageSource"
          p:basename="i18n/messages"/>

    <bean id="validator"
          class="org.springframework.validation.beanvalidation.LocalValidatorFactoryBean"
          p:providerClass="org.hibernate.validator.HibernateValidator"
          p:validationMessageSource-ref="messageSource"/>

    <!-- 支持上传文件 -->
    <bean id="multipartResolver"
          class="org.springframework.web.multipart.commons.CommonsMultipartResolver"
          p:defaultEncoding="UTF-8" p:maxUploadSize="1024000000"
          p:resolveLazily="true" p:maxInMemorySize="4096"/>
</beans>
